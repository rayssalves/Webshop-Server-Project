import "./style.css";
import { NavLink } from "react-router-dom";

const NavBar = () => {
  return (
    <nav>
      <ul>
        <li>
          <NavLink></NavLink>
        </li>
      </ul>
    </nav>
  );
};

export { NavBar }; //reader asks to do like this
